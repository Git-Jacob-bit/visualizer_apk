<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ system splash: the launcher mark on the app's ink ground. -->
    <style name="Theme.Wizualizator" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowSplashScreenBackground">@color/window_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launcher_foreground</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/launcher_background</item>
    </style>
</resources>
